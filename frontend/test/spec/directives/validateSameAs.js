'use strict';

describe('Directive: validateSameAs', function () {
  beforeEach(module('TreexWebApp'));

  var element;

  // it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //   element = angular.element('<validate-same-as></validate-same-as>');
  //   element = $compile(element)($rootScope);
  //   expect(element.text()).toBe('this is the validateSameAs directive');
  // }));
});
