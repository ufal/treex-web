'use strict';

describe('Directive: twTimer', function () {
  beforeEach(module('TreexWebApp'));

  var element;

  // it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //   element = angular.element('<tw-timer></tw-timer>');
  //   element = $compile(element)($rootScope);
  //   expect(element.text()).toBe('this is the twTimer directive');
  // }));
});
